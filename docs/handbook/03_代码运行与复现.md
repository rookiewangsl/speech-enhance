# 代码运行与复现

最后更新：2026-08-14

## 1. 环境

```bash
cd /Users/shilongwang/Library/CloudStorage/Dropbox/Code/realtime_speech_enhancement

python3 -m venv .venv
./.venv/bin/pip install -e '.[data,dev,evaluation,demo]'
./scripts/setup_rnnoise.sh
./.venv/bin/python -m pytest
```

Whisper 使用独立环境，避免把 PyTorch 强制加入轻量 DSP 环境：

```bash
python3.12 -m venv .venv-asr
./.venv-asr/bin/pip install -e '.[asr]'
```

仓库内 `.venv/bin/pytest` 的 shebang 可能随目录迁移失效，统一使用：

```bash
./.venv/bin/python -m pytest
```

## 2. 单文件 RNNoise

输入必须是 16/48 kHz mono WAV：

```bash
./.venv/bin/python scripts/enhance_rnnoise.py \
  --input input.wav \
  --output output_rnnoise.wav \
  --mode official \
  --compensate-delay \
  --vad-json output_vad.json
```

`--compensate-delay` 只用于离线 A/B 和 paired metric。实时输出必须保留 10 ms（48 kHz）或
12.625 ms（16 kHz）因果延迟。`--chunk-size 137` 可压力测试非整帧输入；
`--pcm-compatible` 只用于与 official raw-PCM CLI 做量化等价检查。

## 3. 固定离线 Demo

```bash
./.venv/bin/python scripts/run_demo.py \
  --input data/processed/voicebank/noisy/p232_005.wav \
  --clean data/processed/voicebank/clean/p232_005.wav \
  --output-dir outputs/final/demo_p232_005 \
  --chunk-size 137
```

试听顺序：

```text
clean_reference.wav
→ noisy_input.wav
→ r3_official.wav
→ c1_continuity_probe.wav
```

`comparison.png` 观察 waveform/spectrogram/VAD，`report.json` 检查采样率、chunk、延迟、
RTF、peak 和 clipping。C1 只是负消融，不是默认输出。

## 4. MacBook 麦克风 Demo

先列出设备：

```bash
./.venv/bin/python scripts/realtime_mic_demo.py \
  --output-dir outputs/mic_demo \
  --list-devices
```

再使用当前 `in > 0` 的设备编号录制：

```bash
./.venv/bin/python scripts/realtime_mic_demo.py \
  --output-dir outputs/mic_demo \
  --duration 10 \
  --device 2
```

录音链路为 48 kHz mono、480 samples/10 ms callback、persistent RNNoise state。系统没有
AEC，因此不应把扬声器回声当作普通环境噪声。`report.json` 应满足：

```text
dropped_blocks = 0
callback_status = []
processing_rtf < 1
output_clipping_samples = 0
algorithmic_delay_samples = 480 @48k
```

真实麦克风没有同步 clean reference，不计算 SI-SDR/STOI；固定读稿可在人工 transcript 后
计算 WER，否则只做 raw/enhanced A/B、RTF、clipping 和主观检查。

## 5. VoiceBank 全量数据

```bash
./.venv/bin/python scripts/download_voicebank.py \
  --data-root data --subset train28

./.venv/bin/python scripts/prepare_voicebank.py \
  --data-root data \
  --protocol full28 \
  --development-speakers 20 \
  --seed 20260724 \
  --workers 4

./.venv/bin/python scripts/audit_full_protocol.py \
  --project-root . \
  --manifest-root data/manifests \
  --output outputs/full_protocol/data_audit.json
```

完整增强评测示例：

```bash
./.venv/bin/python scripts/evaluate_enhancement.py \
  --manifest data/manifests/validation.jsonl \
  --project-root . \
  --output-root outputs/full_protocol/rnnoise/validation_full \
  --methods rnnoise

./.venv/bin/python scripts/summarize_metrics_by_condition.py \
  --input outputs/full_protocol/rnnoise/validation_full/metrics/rnnoise_metrics.csv \
  --output outputs/full_protocol/rnnoise/validation_full/metrics/by_condition.json
```

大体积 ZIP/WAV、Whisper 权重和可重建缓存保留在：

```text
/Volumes/T7/ProjectData/realtime_speech_enhancement/
```

## 6. ASR reference 与四路输入

先用官方 VCTK transcript 建立独立 reference manifest：

```bash
./.venv/bin/python scripts/prepare_asr_references.py \
  --manifest data/manifests/development.jsonl \
  --manifest data/manifests/validation.jsonl \
  --manifest data/manifests/official_test.jsonl \
  --transcript-root /Volumes/T7/ProjectData/realtime_speech_enhancement/data/raw/vctk_0.92/extracted/txt \
  --output /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/references.jsonl \
  --audit-output /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/reference_audit.json
```

再为一个 partition 生成 `clean/noisy/mcra_dd_wiener/rnnoise_r3` 四路 WAV：

```bash
./.venv/bin/python scripts/export_asr_inputs.py \
  --manifest data/manifests/validation.jsonl \
  --project-root . \
  --output-root /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/validation_inputs
```

导出器校验 sample rate、长度、SHA、增强配置和 RNNoise 延迟补偿，并支持原子缓存、中断续跑。

## 7. ASR v1 固定后端

正式设备固定 CPU + FP32；本机 MPS probe 的 normalized hypothesis 与 CPU 不一致：

```bash
./.venv-asr/bin/python scripts/evaluate_asr.py \
  --inputs /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/validation_inputs/manifests/validation_asr_inputs.jsonl \
  --references /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/references.jsonl \
  --config configs/asr_whisper_small_en.json \
  --project-root . \
  --model-root /Volumes/T7/ProjectData/realtime_speech_enhancement/models/whisper \
  --output /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/validation_cpu/hypotheses.jsonl \
  --cache-root /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/validation_cpu/cache \
  --environment-output /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/validation_cpu/environment.json \
  --device cpu
```

```bash
./.venv/bin/python scripts/summarize_asr_metrics.py \
  --input /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/validation_cpu/hypotheses.jsonl \
  --output /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/validation_cpu/summary.json \
  --utterance-output /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/validation_cpu/per_utterance.jsonl
```

## 8. ASR v2 灾难保护

v2 不重写 v1，读取完整四路 v1 JSONL 后只对 reference-free 异常做固定 temperature 重试：

```bash
./.venv-asr/bin/python scripts/evaluate_asr_robust.py \
  --v1-hypotheses /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/validation_cpu/hypotheses.jsonl \
  --config configs/asr_whisper_small_en_robust_v2.json \
  --base-config configs/asr_whisper_small_en.json \
  --project-root . \
  --model-root /Volumes/T7/ProjectData/realtime_speech_enhancement/models/whisper \
  --output /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_robust_v2/validation_cpu/hypotheses.jsonl \
  --cache-root /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_robust_v2/validation_cpu/cache \
  --environment-output /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_robust_v2/validation_cpu/environment.json \
  --device cpu
```

```bash
./.venv/bin/python scripts/summarize_asr_robust.py \
  --v1 /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_v1/validation_cpu/hypotheses.jsonl \
  --v2 /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_robust_v2/validation_cpu/hypotheses.jsonl \
  --output /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_robust_v2/validation_cpu/summary.json \
  --utterance-output /Volumes/T7/ProjectData/realtime_speech_enhancement/outputs/asr/asr_whisper_small_en_robust_v2/validation_cpu/per_utterance.jsonl
```

完全缓存复跑应显示输入行数全部命中，且 `retry_inferences=0`、`model_loads=0`。

## 9. 常见问题

- 找不到 RNNoise 共享库：运行 `./scripts/setup_rnnoise.sh`。
- 找不到麦克风依赖：运行 `./.venv/bin/pip install -e '.[demo]'`。
- 输出比输入晚：这是因果延迟，不是文件损坏。
- SI-SDR 极低：先检查是否补偿完整 202 samples，而不是只补 FIR 延迟。
- ASR 缓存未命中：检查音频、模型、配置、evaluator 和 runtime identity SHA。
- MPS 更快但文本不同：正式结果继续使用 CPU，速度不能覆盖身份不一致。
